<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Course Created Successfully</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background-color: #f9f9f9;
      text-align: center;
    }
    .success-box {
      background-color: #fff;
      border-radius: 12px;
      display: inline-block;
      padding: 2rem;
    }
    img {
      max-width: 300px;
      border-radius: 8px;
      margin-top: 1rem;
    }
    .course-id {
      color: #888;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <div class="success-box">
    <h1>✅ Course Created Successfully!</h1>
    <p class="course-id">Course ID: <strong><%= course.id %></strong></p>
    <h2><%= course.title %></h2>
    <% if (course.imageUrl) { %>
      <img src="<%= course.imageUrl %>" alt="Course Image" />
    <% } else { %>
      <p>No image uploaded.</p>
    <% } %>

    <div><a href="/">View All Courses</a></div>
  </div>
</body>
</html>
